object Main {
    def main(args: Array[String]) {
        val indice = io.StdIn.readLine().toInt
        val grito = "a" * indice
        println("Ent" + grito + "o eh N" + grito + "t" + grito + "l!")
    }
}
